# 📋 TASK-001-002: Tabla settings + IPC handlers settings:*

**TASK-001-002**: Tabla `settings` y handlers IPC para configuración global
**Épica**: EPIC-001
**Prerrequisito**: TASK-001-001
**Duración**: 1h
**Estado**: ✅ Completada

---

## 📖 Descripción
La tabla `settings (key TEXT PK, value TEXT)` persiste la configuración del negocio.
Los handlers IPC `settings:get`, `settings:set`, `settings:getAll`, `settings:setBulk`
permiten al Renderer leer y escribir configuración de forma atómica.

## ✅ Criterios de Aceptación
- [x] `settings:getAll` retorna un objeto `Record<string, string>` con todas las keys
- [x] `settings:setBulk({ key: value })` guarda múltiples valores en una sola transacción
- [x] `settings:get("store_name")` retorna el valor o `null` si no existe

## ✅ Completada
- [x] Código en `src/main/ipc-handlers.js` (handlers settings:*)
- [x] Valores por defecto insertados en migración v2
